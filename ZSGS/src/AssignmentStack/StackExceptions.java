package AssignmentStack;

import java.util.Scanner;

/*
In - case of Integer values :-

As int data type is 32 bit in Java.
If we reach the minimum value JVM doesn't throw any exception. Instead, it rolls over to the maximum value.

Integer.MIN_VALUE = -2147483648 - 1       -->         Integer.MAX_VALUE = 2147483647

For non-integer based data types, the underflow result in ZERO values.
*/

public class StackExceptions {
	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter the element");
	}

}
